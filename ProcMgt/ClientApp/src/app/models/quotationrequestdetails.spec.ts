import { Quotationrequestdetails } from './quotationrequestdetails';

describe('Quotationrequestdetails', () => {
  it('should create an instance', () => {
    expect(new Quotationrequestdetails()).toBeTruthy();
  });
});
