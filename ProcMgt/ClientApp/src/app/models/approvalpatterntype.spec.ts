import { Approvalpatterntype } from './approvalpatterntype';

describe('Approvalpatterntype', () => {
  it('should create an instance', () => {
    expect(new Approvalpatterntype()).toBeTruthy();
  });
});
