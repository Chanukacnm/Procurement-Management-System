import { Quotationerequestheader } from './quotationerequestheader';

describe('Quotationerequestheader', () => {
  it('should create an instance', () => {
    expect(new Quotationerequestheader()).toBeTruthy();
  });
});
